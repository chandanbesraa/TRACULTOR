/**
 * Initial empty data structures for clean application startup.
 * The application starts completely clean with 0 sample records.
 */

export const INITIAL_CUSTOMERS_QUEUE = [];

export const INITIAL_COMPLETED_RECORDS = [];
